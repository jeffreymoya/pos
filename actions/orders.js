import { createAction } from 'redux-starter-kit';

export const update = createAction('order/update');
export const add = createAction('order/add');
export const remove = createAction('order/remove');
